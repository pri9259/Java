import java.io.*;
import java.util.*;

public class Solution {

    public static void main(String[] args) {
        Scanner s=new Scanner(System.in);
        String java=s.nextLine();
        StringBuilder sb=new StringBuilder(java);
        sb.reverse();
        System.out.println(sb);
    }
}
